#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: the second number to multiply with the first number
 *
 * Return: The result oof multiplication between x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
